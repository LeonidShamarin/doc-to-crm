google-genai>=2.17,<3
pydantic>=2.9,<3
python-dotenv>=1.0
truststore>=0.10
fastapi>=0.115
uvicorn[standard]>=0.32
python-multipart>=0.0.9
httpx>=0.27

# Генерація синтетичного датасету. reportlab дає PDF із текстовим шаром
# («цифровий» рахунок), Pillow — растрові «скани»: поворот, шум, JPEG-артефакти.
# Обидва — легкі чисті залежності, без системних бібліотек.
reportlab>=4.2
pillow>=10.4

pytest>=8.3
pytest-asyncio>=0.24
